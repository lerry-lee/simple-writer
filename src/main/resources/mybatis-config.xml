<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <properties resource="jdbc.properties"></properties>

    <settings>
        <!--当返回行的所有列都是空时，MyBatis默认返回null-->
        <setting name="returnInstanceForEmptyRow" value="true"/>
    </settings>

    <typeAliases>
        <package name="com.writer1.entity"></package>
    </typeAliases>
    <!--使用mybatis分页插件-->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor"></plugin>
    </plugins>

    <environments default="development">

        <environment id="development">
            <!--使用了 JDBC 的提交和回滚设置，它依赖于从数据源得到的连接来管理事务作用域-->
            <transactionManager type="JDBC"/>
            <!--使用数据池，复用实例-->
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mybatis/User.xml"></mapper>
        <mapper resource="mybatis/Report.xml"></mapper>
        <mapper resource="mybatis/Reflective.xml"></mapper>
        <mapper resource="mybatis/Posts.xml"></mapper>
        <mapper resource="mybatis/Comments.xml"></mapper>
        <mapper resource="mybatis/Messages.xml"></mapper>
        <mapper resource="mybatis/Visits.xml"></mapper>
    </mappers>
</configuration>